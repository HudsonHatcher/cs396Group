# pytest.ini - Configuration for running tests
# Defines where tests are located and how they should be executed

[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --verbose --tb=short
